# Ingestion Worker Dependencies
# Core dependencies
asyncpg==0.29.0
python-dotenv==1.0.0
httpx>=0.27.0

# Ingestion pipeline dependencies (from rag-app)
# These are needed to run DocumentIngestionPipeline
docling>=2.55.0
sentence-transformers>=3.3.0
transformers>=4.47.0
torch>=2.5.0
PyMuPDF==1.25.1  # For extracting images from PDFs

# OCR engines for Docling
rapidocr-onnxruntime>=1.3.25  # RapidOCR (recommended, ~2x faster than EasyOCR)
pytesseract>=0.3.10  # Tesseract OCR Python wrapper (requires tesseract-ocr system package)

# VLM (Vision Language Model) engines for image analysis
paddleocr==2.6.1.3  # PaddleOCR-VL (stable version, 2.7+ has breaking API changes)
paddlepaddle==2.5.2  # PaddlePaddle backend (CPU version, compatible with paddleocr 2.6.x)
