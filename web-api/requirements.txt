fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
slowapi==0.1.9
python-dotenv==1.0.1
asyncpg==0.30.0
pydantic>=2.10,!=2.10.0,!=2.10.1,!=2.10.2
pydantic-settings==2.6.0
pydantic-ai==0.0.18
httpx==0.28.1
aiofiles==24.1.0
reportlab==4.2.5
markdown==3.7
rich==13.9.4
tqdm==4.67.1
pymupdf==1.24.14  # PyMuPDF for PDF annotation (PDF highlighting feature)

# Heavy ingestion packages (docling, transformers, tiktoken) NOT needed
# Backend only does runtime operations: API, database queries, vector search
# Ingestion is done locally via rag-app container

# Testing dependencies moved to CI/CD environment (GitHub Actions)
# Install locally with: pip install pytest pytest-asyncio pytest-cov pytest-mock
