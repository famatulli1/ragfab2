# Core dependencies
asyncpg==0.30.0
pydantic-ai==0.0.18
python-dotenv==1.0.1
httpx==0.28.1
pydantic==2.9.2  # Downgraded to avoid conflict with docling-core
rich==13.9.4

# Ingestion dependencies
transformers==4.57.0
tiktoken==0.8.0
docling==2.15.0
# docling-core will be installed automatically with correct version

# Additional utilities
tqdm==4.67.1
