# Core dependencies
asyncpg==0.30.0
pydantic-ai==0.0.18
python-dotenv==1.0.1
httpx==0.28.1
pydantic>=2.10,!=2.10.0,!=2.10.1,!=2.10.2  # Avoid versions excluded by docling-core
rich==13.9.4

# Ingestion dependencies
transformers==4.57.0
tiktoken==0.8.0
docling==2.15.0
# docling-core will be installed automatically with correct version
PyMuPDF==1.25.1  # For extracting images from PDFs

# OCR engines for Docling
rapidocr-onnxruntime>=1.3.0  # RapidOCR (recommended, ~2x faster than EasyOCR)
paddleocr>=2.7.0  # PaddleOCR-VL for advanced OCR and layout analysis
paddlepaddle>=2.6.0  # CPU version - use paddlepaddle-gpu for GPU support
pytesseract>=0.3.10  # Tesseract OCR Python wrapper (requires tesseract-ocr system package)
Pillow>=10.0.0  # Image processing for PaddleOCR

# Additional utilities
tqdm==4.67.1

# Testing dependencies moved to CI/CD environment (GitHub Actions)
# Install locally with: pip install pytest pytest-asyncio pytest-cov pytest-mock
